function calculateArea(width, height) {
    const area = width * height;
    return area;
}
const sampleWidth = 10;
const sampleHeight = 6;
const result = calculateArea(sampleWidth, sampleHeight);
console.log("The area of the reactangle is " + result);